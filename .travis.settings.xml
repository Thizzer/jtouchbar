<settings>
    <servers>
        <server>
            <id>maven.central.release</id>
            <password>${env.MAVEN_CENTRAL_TOKEN}</password>
        </server>
        <server>
            <id>maven.central.snapshot</id>
            <password>${env.MAVEN_CENTRAL_TOKEN}</password>
        </server>
    </servers>
</settings>

